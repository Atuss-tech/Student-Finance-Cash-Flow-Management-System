<Window x:Class="WPF.Features.Transactions.AllTransactionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tất cả giao dịch" Height="550" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#f0f2f5">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Tất cả giao dịch" FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>
        
        <Border Grid.Row="1" Background="#ffffff" BorderBrush="#e5e7eb" BorderThickness="1" CornerRadius="12" Padding="8">
            <ListView ItemsSource="{Binding AllTransactions}" BorderThickness="0" Background="Transparent"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="12,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="{Binding IconBackground}" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" Foreground="#111827"/>
                                <TextBlock Text="{Binding Category}" FontSize="14" Foreground="#6b7280" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <!-- Date -->
                            <TextBlock Grid.Column="2" Text="{Binding FormattedDate}" FontSize="14" Foreground="#6b7280" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            
                            <!-- Amount -->
                            <TextBlock Grid.Column="3" Text="{Binding FormattedAmount}" FontSize="16" FontWeight="Bold" Foreground="{Binding AmountColorBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#f3f4f6"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>
    </Grid>
</Window>
