<UserControl x:Class="WPF.Views.UserControls.RecentTransactionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Margin="0,0,0,12">
    <Border Background="Transparent" CornerRadius="10" Padding="0">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#08FFFFFF"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Border Grid.Column="0" Width="44" Height="44" CornerRadius="12"
                    Background="{Binding IconBackground}" Margin="0,0,14,0">
                <TextBlock Text="{Binding Icon}" HorizontalAlignment="Center"
                           VerticalAlignment="Center" FontSize="20"/>
            </Border>

            <!-- Title + Category -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14"
                           Foreground="{DynamicResource TextPrimaryBrush}" FontFamily="Segoe UI"/>
                <TextBlock Text="{Binding Category}" FontSize="12"
                           Foreground="{DynamicResource TextMutedBrush}"/>
            </StackPanel>

            <!-- Amount + Date -->
            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                <TextBlock Text="{Binding FormattedAmount}" FontWeight="Bold" FontSize="14"
                           Foreground="{Binding AmountColorBrush}" HorizontalAlignment="Right"
                           FontFamily="Segoe UI"/>
                <TextBlock Text="{Binding FormattedDate}" FontSize="11"
                           Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
