# Data Model: Navigate to Transactions from Dashboard

No new data models or entities are required for this feature. This is a purely UI navigation feature that utilizes the existing `MainWindow` and `DashboardHomeView` structures.
